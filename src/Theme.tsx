import { createTheme } from "@mui/material";

export const theme = createTheme({
    colorSchemes: {
        dark: {
            palette: {
                background: {
                    paper: "#222222ff",
                    default: "#222222ff",
                }
            }
        },
        light: {
            palette: {
                background: {
                    paper: "#ffffffff",
                    default: "#ffffffff",
                }
            }
        }
    },

    
})